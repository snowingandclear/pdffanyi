# pdffanyi 使用文档

高精度 PDF 翻译工具:日文绘画教程 PDF → 简体中文,保留原版式、图片,描图级覆盖重绘。

## 1. 环境准备

```bash
# 系统依赖 (Termux)
pkg install -y poppler tesseract python-numpy freetype

# tesseract 语言包 (tessdata_best 版,放至 $PREFIX/share/tessdata/):
#   jpn / jpn_vert / chi_sim / chi_sim_vert
#   来源: https://github.com/tesseract-ocr/tessdata_best

# Python 依赖
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple pillow openai requests
```

## 2. 翻译引擎

支持 4 种引擎,启动时**交互式选择**(见下),也可用参数或配置文件指定:

| 编号 | 引擎 | 说明 | key |
|---|---|---|---|
| 1 | `google` | 免费接口,无需 key | 无 |
| 2 | `youdao` | 有道免费接口,国内直连 | 无 |
| 3 | `llm` | OpenAI 兼容 API(如 DeepSeek),质量最好,支持上下文 | 需要 |
| 4 | `opencode` | 本机 `opencode serve`,可作兜底,支持上下文 | 复用 opencode 登录 |

### 上下文翻译

LLM 和 OpenCode 引擎支持上下文翻译,会保留最近 36 行(3批)译文作为上下文,保持前后文连贯性。Google 和 Youdao 免费引擎不支持上下文。

### 配置文件 `.env.local`(可选,已 gitignore)

项目根目录建 `.env.local`,llm 引擎会自动读取(命令行输入的 key 优先):

```bash
LLM_API_KEY=sk-xxxxxxxx
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-v4-flash
LLM_TEMPERATURE=0.2          # 可选
LLM_REQUEST_GAP=1.2          # 可选, 两次请求间隔秒数
LLM_FALLBACK_ENGINE=opencode # 可选, 主引擎失败后兜底 (默认 opencode)
OPENCODE_URL=http://127.0.0.1:4096  # 可选
```

### opencode 兜底(可选)

主引擎(llm)连续失败 2 次后自动改用本机 opencode 翻译,先启动 serve:

```bash
opencode serve                 # 默认 127.0.0.1:4096
```

兜底不需要额外 key,直接用 opencode 登录过的模型。

## 3. 基本用法

```bash
cd ~/projects/AiSmallTools/pdffanyi

# 交互式: 先选引擎, 再输入 AI key (仅在终端中; 后台/管道运行自动跳过)
python3 -m pdf_translate.pipeline 输入.pdf -o 输出.pdf

# 选择流程示例:
#   选择翻译引擎:
#     1) google      免费, 无需 key
#     2) youdao      免费, 无需 key
#     3) llm         OpenAI 兼容 API (如 DeepSeek)
#     4) opencode    本机 opencode serve
#   请输入编号 (回车默认 1): 1

# 非交互 (脚本/后台任务用, 参数齐全)
python3 -m pdf_translate.pipeline 输入.pdf --engine google -o 输出.pdf

# 使用 LLM 引擎 (质量最好, 需要 API key)
python3 -m pdf_translate.pipeline 输入.pdf --engine llm \
  --api-key sk-xxxx --base-url https://api.deepseek.com \
  --model deepseek-v4-flash -o 输出.pdf

# 只翻第 5~10 页(建议先小范围试跑)
python3 -m pdf_translate.pipeline 输入.pdf --start 5 --end 10 --debug
```

## 4. 参数说明

| 参数 | 默认 | 说明 |
|---|---|---|
| `-o, --output` | `输入名_translated.pdf` | 输出路径 |
| `--start / --end` | 全部 | 页码范围(从 1 开始) |
| `--dpi` | 216 | 渲染分辨率,越高 OCR 越准但越慢、文件越大 |
| `--lang` | `jpn+chi_sim` | OCR 语言,中文书用 `chi_sim+jpn` |
| `--psm` | 11 | tesseract 版面模式,11=稀疏文本(适合画册) |
| `--debug` | 关 | 保存 `_debug_N.png` 调试图,并保留中间 jpg |
| `--fresh` | 关 | 丢弃已有断点从第 1 页重翻 |
| `--engine` | `google` | `google` / `youdao` / `llm` / `opencode` |
| `--api-key` | `.env.local` | AI 引擎 API key |
| `--base-url` | `.env.local` | AI 引擎 OpenAI 兼容接口地址 |
| `--model` | `.env.local` | AI 引擎模型名 |
| `--fallback` | `opencode` | 主引擎失败后的兜底引擎;`--fallback none` 关闭 |
| `--opencode-url` | `127.0.0.1:4096` | opencode serve 地址 |

> 提示:传了任意引擎相关参数(`--engine` 等)就跳过交互式选择,适合脚本/后台任务。

## 5. 断点续传

翻译过程中每完成一页会自动保存到 `<输出名>_checkpoints/` 目录,包括:
- `meta.json`: 记录参数和已完成页码
- `p{页码}.jpg`: 已翻译的页面图片

如果翻译中断(断电/余额不足/进程被杀),重新运行**相同参数**的命令会自动跳过已完成页,继续翻译。

- 使用 `--fresh` 参数可强制从第 1 页重新翻译
- 如果参数不匹配(如修改了 DPI),会自动清空旧断点重新开始

## 6. 智能过滤

系统会自动识别并过滤不需要翻译的内容:

| 类型 | 说明 |
|---|---|
| 插画区 | 自动检测彩色插画区域,不翻译其中的文字 |
| 对话气泡 | 人物对话短句不翻译 |
| 版权水印 | 网站水印文字(如「素材工坊 www.cgartist.net」)不翻译 |
| 软件界面 | Photoshop/SAI 等软件界面截图内的文字不翻译 |
| 目录页 | 手动配置 `TOC_FORCE_REGIONS_MANUAL` 强制翻译目录条目 |

## 7. 调优建议

- **先跑 1~2 页 `--debug`**,用 `_debug_N.png` 检查:
  - 有没有漏翻/误擦:调整 `config.py` 的 `OCR_CONFIDENCE_THRESHOLD`(0.55,越高越严格)
  - 文字颜色:白字彩底自动检测,`MIN_TEXT_COLOR_GAP`(40)是判定阈值
- 识别率低但版面正常的书:加 `--dpi 300`(慢 2~3 倍)
- 一行太长被截断:竖排自动缩字号(最小 `FONT_SIZE_MIN`=10),横排自动换行
- 翻译质量优先:用 llm(DeepSeek)而非 google/youdao;llm 支持上下文,连贯性更好
- 免费引擎被限流:稍等重试,或切换到 llm 引擎

## 8. 已知限制

- 输出为整页图片 PDF(不可选中复制文字,体积较大)
- 花体装饰字/手绘标注类文字 OCR 识别率低,属商用 OCR 也难处理的场景
- 纯图片页无文字框则原样透传
- llm 引擎失败重试不扣费,但会拖慢进度;失败次数过多时自动启用 opencode 兜底
- 免费引擎(Google/Youdao)不支持上下文翻译,一词多义可能翻错

## 9. 常见问题

| 现象 | 解决 |
|---|---|
| `poppler not installed` | `pkg install poppler` |
| `tesseract not installed` | `pkg install tesseract` |
| 翻译为空 | 接口限流,稍等重试;或用 llm 引擎 |
| llm 报限流/超时 | 自动重试+退避,连续失败会切 opencode 兜底 |
| 兜底 opencode 连接失败 | `opencode serve` 未启动,先运行它 |
| OCR 结果为 0 框 | 页面过小,提高 `--dpi`;或语言不对,换 `--lang` |
| 翻译全是原文 | 原文为专有名词/无法翻译的内容,属预期 |
| 翻译不连贯 | 切换到 llm 引擎,支持上下文翻译 |

## 10. 目录结构

```
pdffanyi/
├── config.py                    # 配置(阈值/字体/字号/引擎)
├── .env.local                   # AI key 等(本地,不进 git)
├── requirements.txt             # Python 依赖
├── 使用文档.md                  # 本文档
├── README.md                    # 项目简介
├── tests/                       # 测试(引擎 mock 集成测试)
└── pdf_translate/
    ├── __init__.py              # 包初始化
    ├── ocr_engine.py            # OCR引擎(Tesseract TSV识别)
    ├── layout.py                # 文本行布局(区域合并/横竖排分组)
    ├── page_filter.py           # 页面过滤(插画/水印/对话气泡)
    ├── translator.py            # 翻译引擎(google/youdao/llm/opencode)
    ├── renderer.py              # 渲染器(背景擦除/文字颜色检测/译文绘制)
    ├── pdfwriter.py             # PDF生成(图片合成PDF)
    └── pipeline.py              # 主流程+CLI(断点续传/交互式选择)
```
