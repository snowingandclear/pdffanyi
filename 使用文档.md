# pdffanyi 使用文档

高精度 PDF 翻译工具:日文绘画教程 PDF → 简体中文,保留原版式、图片,描图级覆盖重绘。

## 1. 环境准备

```bash
# 系统依赖 (Termux)
pkg install -y poppler tesseract python-numpy freetype

# tesseract 语言包 (tessdata_best 版,放至 $PREFIX/share/tessdata/):
#   jpn / jpn_vert / chi_sim / chi_sim_vert
#   来源: https://github.com/tesseract-ocr/tessdata_best

# Python 依赖
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple pillow openai requests
```

## 2. 配置

翻译使用**有道免费接口**（`https://aidemo.youdao.com/trans`），**无需 API key**，国内直连即可用，不需要任何环境变量。

## 3. 基本用法

```bash
cd ~/projects/AiSmallTools/pdffanyi

# 整本翻译
python3 -m pdf_translate.pipeline 输入.pdf -o 输出.pdf

# 只翻第 5~10 页(建议先小范围试跑)
python3 -m pdf_translate.pipeline 输入.pdf --start 5 --end 10 --debug
```

## 4. 参数说明

| 参数 | 默认 | 说明 |
|---|---|---|
| `-o, --output` | `输入名_translated.pdf` | 输出路径 |
| `--start / --end` | 全部 | 页码范围(从 1 开始) |
| `--dpi` | 216 | 渲染分辨率,越高 OCR 越准但越慢、文件越大 |
| `--lang` | `jpn+chi_sim` | OCR 语言,中文书用 `chi_sim+jpn` |
| `--psm` | 11 | tesseract 版面模式,11=稀疏文本(适合画册) |
| `--debug` | 关 | 保存 `_debug_N.png` 调试图,并保留中间 jpg |

## 5. 调优建议

- **先跑 1~2 页 `--debug`**,用 `_debug_N.png` 检查:
  - 有没有漏翻/误擦:调整 `config.py` 的 `OCR_CONFIDENCE_THRESHOLD`(0.55,越高越严格)
  - 文字颜色:白字彩底自动检测,`MIN_TEXT_COLOR_GAP`(40)是判定阈值
- 识别率低但版面正常的书:加 `--dpi 300`(慢 2~3 倍)
- 一行太长被截断:竖排自动缩字号(最小 `FONT_SIZE_MIN`=10),横排自动换行

## 6. 已知限制

- 输出为整页图片 PDF(不可选中复制文字,体积较大)
- 花体装饰字/手绘标注类文字 OCR 识别率低,属商用 OCR 也难处理的场景
- 纯图片页无文字框则原样透传

## 7. 常见问题

| 现象 | 解决 |
|---|---|
| `poppler not installed` | `pkg install poppler` |
| `tesseract not installed` | `pkg install tesseract` |
| 翻译为空 | 有道接口限流,稍等重试 |
| OCR 结果为 0 框 | 页面过小,提高 `--dpi`;或语言不对,换 `--lang` |
| 翻译全是原文 | 原文为专有名词/无法翻译的内容,属预期 |

## 8. 目录结构

```
pdffanyi/
├── config.py                    # 配置(阈值/字体/字号)
├── requirements.txt
├── 使用文档.md                  # 本文档
├── README.md                    # 项目简介
├── tests/                       # 测试产物
└── pdf_translate/
    ├── ocr_engine.py            # Tesseract TSV OCR
    ├── layout.py                # 行分组(横/竖排)
    ├── translator.py            # 有道免费批量翻译+重试+编号对位
    ├── renderer.py              # 背景采样擦除+文字颜色检测+排版重绘
    ├── pdfwriter.py             # 手写 PDF 生成
    └── pipeline.py              # 主流程 + CLI
```